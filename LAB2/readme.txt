HOW TO RUN THE PROGRAM:

run : python LAB2.py
will print to console the words and the newid of the articles body that contain at least one instance of the word
currently is done for a single text file
would need to loop on each file but logic remains the same
